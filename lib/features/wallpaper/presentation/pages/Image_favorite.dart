import 'package:flutter/material.dart';

class ImageFavorites extends StatelessWidget {
  const ImageFavorites({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
